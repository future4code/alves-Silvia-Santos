 
 ```function calculaNota(ex, p1, p2) {
  let A 
  let B
  let C
  let D
  let nota = ex + p1 + p2 
  let nota1 = nota / 3
  leto nota2 = nota1
   if (nota2 >= 9 ){
      abcd = A
     } else if (nota2 >= 7.5){
       abcd = B
     } else if (nota2 >= 6){
       abcd = C
     } else (nota2 < 6){
       abcd = D
     }
       let nota2 = A, B, C, D
       return nota2
  //return resul
}```